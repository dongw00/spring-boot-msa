server:
  port: 7001
  error:
    include-message: always
spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: product_db

logging:
  level:
    root: INFO
    se.magnus: DEBUG